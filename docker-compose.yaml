services:
  app:
    build: .
    container_name: gocandidit-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
    # mount SQLite db directory so data persists across container restarts
      - sqlite_data:/app/database
    restart: unless-stopped

volumes:
  sqlite_data: